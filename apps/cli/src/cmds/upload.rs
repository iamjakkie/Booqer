pub fn handle_upload(path: String) -> anyhow::Result<()> {
    // 1. Hash + upload to S3
    // 2. Extract text from first 5 pages
    // 3. Infer metadata
    // 4. Show to user
    // 5. Send to backend

    Ok(())
}
